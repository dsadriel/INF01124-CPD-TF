#include <b_tree.h>
#include <file_manager.h>
#include <stdio.h>

int main() {

    // Cadastro de novos agendamentos, pacientes e médicos; -->> novo.c
    // Atualização de agendamentos, pacientes e médicos; -->> editar.c
    // --- TODO: Fazer a adição de registros no arquivo de dados e nos índices 

    // Listagem por data de agendamento, data de consulta, etc.;
    /// --- Usar uma árvore B para ordenar os agendamentos por data
    /// ------ Usar um posting list para indexar os agendamentos de um mesmo dia
    
    // Filtro por médico, idade, agendamentos já realizados, etc.;
    /// ???? 
    

    // Análise dos não comparecimentos dos pacientes nos agendamentos das possíveis relações com os
    // dados etnográficos.

    return 0;
}